@using EduHome.Core.Entities;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model IEnumerable<Teachers>
<partial name="_Notification" />
@{
	ViewData["Title"] = "Index";
	int count = 1;
}
<div class="container-fluid">

	<!-- start page title -->
	<div class="row">
		<div class="card">
			<div class="card-body">
				<h4 class="card-title">Sliders</h4>
				<a asp-controller="Teachers" asp-action="Create" class="btn btn-primary mb-4">Create New Teacher</a>
				<div class="table-responsive">
					<table class="table table-dark mb-0">

						<thead>
							<tr>
								<th>#</th>
								<th>Name</th>
								<th>Position</th>
								<th>ImagePath</th>
								<th>Action</th>
							</tr>
						</thead>
						<tbody>
							@foreach (var teacher in Model)
							{
								<tr>
									<th scope="row">@count</th>
									<td> <img src="/@teacher.ImagePath" style="width:100px" /> </td>
									<td>@teacher.Name</td>
									<td>@teacher.Position</td>
									<td>
										<a asp-action="Update" asp-route-id="@teacher.Id" class="btn btn-warning mb-4">	<i class="bi bi-pencil-square"></i></a>
										<a asp-action="Delete" asp-route-id="@teacher.Id" class="btn btn-danger mb-4"><i class="bi bi-trash-fill"></i></a>
									</td>
								</tr>
								count++;
							}

						</tbody>
					</table>
				</div>

			</div>
		</div>
	</div>
</div>